apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -dev

namePrifix: -app1

labels:
  - pairs:
      env: dev
      includeSeclectors: true

replicas:
  - name: Nginx-Deploy
    count: 2