apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -stage

namePrifix: -app1

labels:
  - pairs:
      env: stage
      includeSeclectors: true

replicas:
  - name: Nginx-Deploy
    count: 3