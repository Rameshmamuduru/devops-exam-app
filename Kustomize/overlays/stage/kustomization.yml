apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -stage

namePrefix: -app1

labels:
  - pairs:
      env: stgae
    includeSelectors: true

replicas:
  - name: Nginx-Deploy
    count: 3